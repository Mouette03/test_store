services:
  firefox:
    image: 'lscr.io/linuxserver/firefox:latest'
    volumes:
      - '${APP_DATA_DIR}/data/config:/config'
    environment:
      - 'PUID=1000'
      - 'PGID=1000'
      - 'TZ=Etc/UTC'
    x-runtipi:
      internal_port: 3001
      is_main: true
x-runtipi:
  schema_version: 2
