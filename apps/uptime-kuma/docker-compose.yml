services:
  uptime-kuma:
    image: 'ghcr.io/louislam/uptime-kuma:2.5.4'
    volumes:
      - '${APP_DATA_DIR}/data:/app/data'
    x-runtipi:
      internal_port: 3001
      is_main: true
x-runtipi:
  schema_version: 2
